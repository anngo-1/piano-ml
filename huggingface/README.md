# Hugging Face

This is just a staging folder for the Space.

Copy `huggingface/space/` into the Space repo, then put the model files at:

```text
models/remi-modern-2048-ft/
```

Do not copy `data/`, `outputs/`, or training runs.
